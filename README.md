# K-Nearest-Neighbors-KNN-
Implementation of the K-Nearest Neighbors (KNN) algorithm on multiple datasets to analyze its performance across different data types. Includes data preprocessing, model training, evaluation, and visualization using Python and scikit-learn.
